<%= form_with(model: current_user.active_relationships.build, local: true) do |form| %>
<div><%= hidden_field_tag :followed_id, @user.id %></div>
<%= form.submit "Follow", class: "button button-main" %>
<% end %>